# TikTok Monitor - Python Dependencies
# Installation: pip install -r requirements.txt

# Core dependencies
yt-dlp>=2023.10.0
packaging>=21.0
# Database (included in Python standard library, but we specify for clarity)
# sqlite3 is already included in Python

# Optional but recommended for advanced features
requests>=2.31.0
